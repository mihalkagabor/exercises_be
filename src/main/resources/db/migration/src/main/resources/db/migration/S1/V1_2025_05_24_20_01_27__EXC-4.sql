CREATE TABLE IF NOT EXISTS body_part(
                                        body_part_id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                        body_part_name VARCHAR(50) NOT NULL UNIQUE

);